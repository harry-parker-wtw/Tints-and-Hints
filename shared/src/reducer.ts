// Placeholder — filled in by Prompt 2 with createRoom, addPlayer,
// applyAction (the pure round/scoring reducer), and viewForPlayer
// (the privacy-filtering function the server uses per connection).
export {};
