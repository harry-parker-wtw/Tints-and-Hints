// Placeholder — filled in by Prompt 2 with Player, Room, Round,
// GamePhase, and the pawn palette.
//
// This constant only exists so the client and server can both import
// something real from `shared` right now, to prove the workspace
// wiring works end-to-end before any real types exist.
export const SHARED_PACKAGE_READY = true;
